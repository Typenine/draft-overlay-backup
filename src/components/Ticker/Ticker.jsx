import React from 'react';
import styles from './Ticker.module.css';

const Ticker = () => {
  return (
    <div className={styles.ticker}>
      {/* Empty container for now - will be populated later */}
    </div>
  );
};

export default Ticker;
